# Dependencies (reinstall with: npm install inside source/)
source/node_modules/

# Electron / electron-builder output
installer/win-unpacked/
installer/.icon-ico/
installer/*.blockmap
installer/builder-debug.yml
installer/__uninstaller-*
dist/
out/

# Keep Setup .exe out of git — attach on GitHub Releases instead
installer/*.exe

# Logs / OS junk
*.log
.DS_Store
Thumbs.db
Desktop.ini
*.tmp
.cache/
