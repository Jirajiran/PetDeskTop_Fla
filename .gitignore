# Dependencies (reinstall with: npm install inside source/)
source/node_modules/

# electron-builder junk (NOT the root Setup .exe — that is committed on purpose)
win-unpacked/
.icon-ico/
*.blockmap
builder-debug.yml
__uninstaller-*
dist/
out/

# Logs / OS junk
*.log
.DS_Store
Thumbs.db
Desktop.ini
*.tmp
.cache/
